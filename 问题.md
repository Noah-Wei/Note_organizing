- **怎么去重？**

  | 方法          | 常用程度 | 性能                   | 可控性 | 说明     |
  | ------------- | -------- | ---------------------- | ------ | -------- |
  | DISTINCT      | ⭐⭐⭐⭐⭐    | 中                     | 低     | 最简单   |
  | GROUP BY      | ⭐⭐⭐⭐     | 中                     | 中     | 可扩展   |
  | ROW_NUMBER    | ⭐⭐⭐⭐⭐    | 中高                   | ⭐⭐⭐⭐⭐  | 最推荐   |
  | ROWID 删除    | ⭐⭐⭐      | 高                     | 中     | 清理数据 |
  | 索引去重      | ⭐⭐⭐⭐     | ⭐⭐⭐⭐⭐                  | 低     | 大表神器 |
  | HASH GROUP BY | ⭐⭐⭐      | ⭐⭐⭐⭐                   | 低     | 避免排序 |
  | UNION         | ⭐⭐       | 低`，`UNION ALL`性能快 | 低     | 自动去重 |

- **`NOT`整体取反逻辑**

  ```sql
  SELECT empno,ename,sal,comm FROM emp
         WHERE sal <= 1500 OR comm IS NULL;
  
  SELECT empno,ename,sal,comm FROM emp
         WHERE NOT (sal>1500 AND comm IS NOT NULL);  
  ```

- **`LIKE` 模糊匹配，`_`匹配任意一个字符和 `%`匹配任意N个字符**

- **`ORDER BY`也可以按照列的位置序列排**

  ```SQL
  SELECT ENAME,EMPNO,SAL FROM emp WHERE sal>1500 ORDER BY 1 DESC;
  
  这里的1表示第一列
  ```

- `UNION` 去重，并且会排序；`UNION ALL` 不去重，并且不会排序

- 集合操作

  | 操作符      | 含义                 | 是否去重 |
  | ----------- | -------------------- | -------- |
  | `UNION`     | 并集                 | ✅ 去重   |
  | `UNION ALL` | 并集                 | ❌ 不去重 |
  | `INTERSECT` | 交集                 | ✅ 去重   |
  | `MINUS`     | 差集，A存在，B不存在 | ✅ 去重   |

- **单引号 `' '` —— 表示字符串；双引号 `" "` —— 表示标识符（对象名）**

- **整体替换`REPLACE`和一一替换`TRANSLATE`**

  - `TRANSLATE`在企业中的实际应用：数字校验

  ```sql
  -- 11.TRANSLATE():用于字符级替换的函数，它按照字符一一对应的方式执行替换操作，与 REPLACE 函数的整体替换不同
  /*
  基本语法
     TRANSLATE(source_string, from_string, to_string)
  参数说明
     source_string: 要进行字符替换的原始字符串
     from_string: 需要被替换的字符集合
     to_string: 替换后的新字符集合
  核心特点
    1. 逐字符替换：对 source_string 中的每个字符单独处理
    2. 位置对应：from_string 和 to_string 按字符位置一一对应
    3. 删除功能：如果 to_string 比 from_string 短，多出的字符将被删除
    4. 大小写敏感：替换区分大小写
  */
  SELECT '数字格式' AS "验证数字格式" FROM dual
  WHERE TRANSLATE('123123897','#0123456789','*') IS NULL;
  -- 纯数字，where判定为null，输出结果
  ```

- **常用的日期函数**

  - `SYSDATE`：当前日期
  - `TO_DATE()`：字符串转日期
  - `TO_CHAR()`：日期转字符串；`FM`去掉前导0；亦可对数字进行格式化
  - `ADD_MONTHS()`：增加月份
  - `MONTHS_BETWEEN()`：月计算
  - `LAST_DAY()`：月末最后一天
  - `NEXT_DAY()`：下一个指定星期
  - `TRUNC()`：截断日期，结果还是日期

- **常数的数值函数**

  - `ROUND()`：四舍五入
  - `TRUNC()`：直接截断
  - `MOD()`：取余数
  - `DBMS_RANDOM`：ORACLE内置的包结构
    - `DBMS_RANDOM.VALUE()`：随机0-1一个小数,不包含1

- **`LEVEL`：伪列，一般结合`CONNECT BY`使用**。<u>未完</u>

  ```sql
  --随机5个10-20的整数
  SELECT trunc(dbms_random.value(10, 20)) FROM dual CONNECT BY LEVEL <= 5;
  ```

- **常用的转换函数**
  - `TO_CHAR()`
  - `TO_NUMBER()`：前置0和后置0将会被去掉；可作为数字判定
  - `TO_DATE()`
- **其他函数**
  - `NVL()`和`NVL2()`：空值处理函数
  - `NULLIF()`：判断两个表达式是否相等。相等返回空，不等返回参数1
  - `DECSODE()`：相当于一个`IF`语句；相当于 `CASE WHEN`语句
- **聚合函数|分组函数|列函数|聚集函数**：会忽略空值
  
  - `SUM()`
  - `AVG()`
  - `MAX()`
  - `MIN()`
  - `COUNT()`
    - `COUNT(1)`=`COUNT(*)`，统计所有行
    - `COUNT(字段)`：统计值不为空的行

- `NULL`+数值= 空值

- **分组查询的执行顺序**

  ```sql
  --书写顺序
  SELECT ...
  FROM ...
  WHERE ...
  GROUP BY ...
  HAVING ...
  ORDER BY ...
  --执行顺序
  1. FROM
  2. JOIN
  3. WHERE
  4. GROUP BY
  5. HAVING
  6. SELECT
  7. DISTINCT
  8. ORDER BY
  ```

- **where后面为什么不能接分组函数**

  - WHERE 发生在 GROUP BY 之前执行，而聚合函数是在分组之后才计算的

- **表连接的方式**

  | 类型     | 关键字     | 返回数据                           |
  | -------- | ---------- | ---------------------------------- |
  | 内连接   | INNER JOIN | 交集，只返回两表中“匹配成功”的数据 |
  | 左连接   | LEFT JOIN  | 左表全部，右表匹配不到的补 NULL    |
  | 右连接   | RIGHT JOIN | 右表全部，左表匹配不到的补 NULL    |
  | 全连接   | FULL JOIN  | 并集，两边都保留，不匹配的补 NULL  |
  | 笛卡尔积 | CROSS JOIN | 全组合（A × B）                    |
  | 自连接   | JOIN 自己  | 逻辑关系                           |

- **`=ANY`等价于`IN`**

- **`> ANY`：保证大于子查询中的任何一个值即可，大于最小值；`ANY`和`ALL`后面必须是子查询**

- **`< ANY`：保证小于子查询中的任何一个值即可，小于最大值**

- **`> ALL`：保证大于子查询中的所有值即可，大于最大值**

- **`< ALL`：保证小于子查询中的所有值皆可，小于最小值**

- `WITH子句`：

  ```sql
  WITH 临时别名1 AS (
  	子查询
  ),
  临时别名2 AS (
  	子查询
  )
  SELECT 。。。
  FROM 。。。;
  ```

- 创建表

  ```
  -- 1.
  CREATE TBALE 表名
  AS select 子句;
  ```
  
  



- 插入数据

  ```
  -- 单条插入
  -- 1.
  INSERT INTO 表 (字段1,字段2) VALUES (值1,值2);
  -- 2.
  INSERT INTO 表 VALUES(字段1值,字段2值, ... 字段N值);
  -- 批量插入
  -- 3.
  INSETY INTO 表 
  	SELECT 子句；
  --
  INSETY INTO 表 (字段1,字段2) 
  	SELECT  (字段1,字段2) 子句；
  ```

  
