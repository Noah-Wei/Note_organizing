> 世界杯小组赛，个球队，,B,C,D，每两个球队，轮询比赛，求比赛组合情况
>

```sql
SELECT t1.tname||'VS'||t2.tname
FROM team t1
JOIN team t2 ON t1.tname<t2.tname
```

> 多表查询哪种写法的效率更高

在 Oracle 中，不同的多表连接写法（如 ANSI JOIN 和旧式 WHERE 连接）在大多数情况下性能是一样的，因为优化器会生成相同的执行计划。真正影响性能的是索引、数据量、连接条件和执行计划。因此企业开发中更推荐使用 ANSI JOIN 提高可读性，而不是为了性能去选择写法。

> 执行计划

执行计划是 Oracle 优化器对 SQL 语句生成的具体执行步骤，包括数据访问路径、连接方式和执行顺序等。它直接决定 SQL 的执行效率。相比 SQL 的写法形式，执行计划更重要，因为不同的执行计划会导致性能差异很大。因此在实际开发中，优化 SQL 的核心就是分析和优化执行计划。

SQL 决定“做什么”，执行计划决定“怎么做”，性能由“怎么做”决定。

> WHERE与ON区别

ON 用于定义表之间的连接条件，而 WHERE 用于对结果进行过滤。在 INNER JOIN 中两者效果类似，但在 LEFT JOIN 中区别很大，如果把右表条件写在 WHERE 中，会导致外连接退化为内连接，因此通常建议将连接条件写在 ON 中，将过滤条件写在 WHERE 中。

INNER JOIN 中 ON ≈ WHERE，但 LEFT JOIN 中差别很大，写错会直接导致数据丢失。

> **LEFT JOIN + IS NULL 用来查“没有匹配的数据”的正确写法**（面试必问）

“LEFT JOIN 保留左表，右表没有匹配时为 NULL。利用 `IS NULL` 条件过滤右表为空的行，即可查出没有匹配的数据。

> **Oracle 中 `WHERE` 和 `ON` 的区别**

在 Oracle（以及 SQL 标准）中，`WHERE` 和 `ON` 都可以用于连接条件，但它们的作用范围不同：

- `ON` 是用于指定 **连接条件**（主要用于 `JOIN` 语句中，决定两表如何匹配）；
- `WHERE` 是用于指定 **筛选条件**（可用于任何查询，用于过滤最终结果）。

**区别总结：**

| 区别点           | `ON`                         | `WHERE`                                  |
| ---------------- | ---------------------------- | ---------------------------------------- |
| 用途             | 指定连接条件（多表如何匹配） | 指定结果过滤条件                         |
| 使用位置         | JOIN 后                      | JOIN 后或单表查询中                      |
| 对连接类型的影响 | 影响结果保留（如外连接）     | 会进一步过滤结果，可能使外连接变成内连接 |

------

所以：在外连接中，**条件写在 ON 里可以保留连接结果的完整性，而 WHERE 会破坏外连接效果**，这就是 `ON` 和 `WHERE` 的核心区别之一。

**简单回答**

`ON` 是给 `JOIN` 语句指定表之间如何连接的条件，`WHERE` 是对最终结果再过滤。尤其在外连接里，用 `WHERE` 会可能把本该保留的记录过滤掉，而写在 `ON` 里面不会，所以 `ON` 条件会影响连接结构，而 `WHERE` 是最终筛选。





